import { FC } from "react";
import styles from "./Layout.module.scss";

const Layout: FC = () => {
    return (
        <div>
            Layout
        </div>
    )
}

export default Layout